version: 1
type: refactoring
name: IsolatedBash
maintainer: Raphael Jenni

primaryParameter:
  id: workingDirPath
  type: folderPath # filePath, folderPath, wildcardPath, string, int, boolean
  defaultValue: .
  description: The working directory for the bash script.

run:
  - id: run_bash
    script:
      - /bin/bash

tests:


documentation:
